## Project Commands

**Build**

`yarn run build:dev`  
`yarn run build:prod` 

**Run**

`yarn run dev-server`  
`yarn run serve`  

**Express**

`yarn start` at localhost:3001

**Test**

`yarn test`  
`yarn test -- --watch`

**Run a Playground Project**

`yarn run file.js`