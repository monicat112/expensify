## Start a Project

The main project  

`yarn run build:dev`  
`yarn run dev-server`

`yarn run build:prod`  
`yarn run serve`  

Testing

`yarn test`
`yarn test -- --watch`

A playground project  
`yarn run ???`
